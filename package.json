{
  "name": "fusebit_interview",
  "version": "2.1.5",
  "description": "A sample project used for interviewing candidates.",
  "main": "libc/index.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node libc/index.js",
    "prettier": "git ls-tree -r master --name-only | grep -E \".[tj]?sx?$\" | grep -v assets | xargs -P 1 prettier --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.801.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "@types/express": "^4.17.9",
    "@types/jsonwebtoken": "^8.5.0",
    "@types/node": "^10.15.3",
    "prettier": "^2.2.1",
    "typescript": "^4.1.2"
  }
}
